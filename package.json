{
  "name": "openclaw-evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "a1:import": "node -e \"import('./src/ingest/transcript-import.js').then(async ({ importTranscriptFile }) => { const transcript = await importTranscriptFile(process.argv[1]); console.log(JSON.stringify(transcript, null, 2)); })\"",
    "a2:run": "node src/cli/run-a2.js"
  }
}